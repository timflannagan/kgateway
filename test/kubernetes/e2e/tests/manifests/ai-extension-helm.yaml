# This file defines the minimal configuration for a user who wants to run kgateway with the AI extension enabled.
# These should be reviewed and tested before being adopted.

# Configuration for the kgateway controller pod
controller:
  logLevel: debug

gateway:
  aiExtension:
    enabled: true
